/* eslint-disable linebreak-style */
export default function getResponseFromAPI() {
  return new Promise((resolve) => {
    const success = true;
    if (success) {
      resolve('true');
    }
  });
}
